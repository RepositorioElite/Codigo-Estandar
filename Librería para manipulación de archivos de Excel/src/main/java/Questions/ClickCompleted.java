package Questions;

/* Ejemplo de question para completar clase click utilizando @Override

    public class ClickCompleted implements Question<ElementAvailability> {
        @Override
        public ElementAvailability answeredBy(Actor actor) {
            return ElementAvailability.from(
                    TodoList.ClickCompleted.resolveFor(actor).isCurrentlyVisible()
            );
        }
}
*/
